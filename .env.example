# 포트폴리오 JSON 파일을 제공하는 CDN 기본 URL (예: https://xxxx.cloudfront.net)
# 값이 없으면 /mock-data(public/mock-data에서 제공)를 사용한다.
# 값이 있으면 position 데이터는 날짜별 경로에서 가져온다:
#   {VITE_DATA_BASE_URL}/positions/{yyyy}-{MM}-{dd}.json
VITE_DATA_BASE_URL=
